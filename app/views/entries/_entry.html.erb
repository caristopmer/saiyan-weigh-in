<p><%= entry.entry_date.strftime("%-m/%-d/%y") %> Scale: <%= entry.weight %> lbs.  Average: <%= entry.average_weight %> lbs. 
<% if entry.user == current_user %>
  <%= link_to "Delete", entry_path(entry.user, entry), method: "delete", data: {confirm: "are you sure you want to delete this entry?"} %>
<% end %>
</p>